<% if current_user %>
  <div><%= link_to current_user.nick_name, '#' %></div>
  <div><%= link_to '注销', logout_path , class: "layui-btn"%></div>
<% else %>
  <div><%= link_to '登录', login_path, class: "layui-btn" %></div>
  <div><%= link_to '注册', signup_path, class: "layui-btn" %></div>
<% end %>